# Resend (https://resend.com) — free tier: 100 emails/day
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Optional: custom "from" address (requires a verified domain on Resend)
# Default uses resend.dev shared domain for testing
# FROM_EMAIL=Velstays <bookings@yourdomain.com>
